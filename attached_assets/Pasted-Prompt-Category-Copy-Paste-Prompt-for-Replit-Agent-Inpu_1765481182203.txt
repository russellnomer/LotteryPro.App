Prompt Category,Copy-Paste Prompt for Replit Agent
Input Validation & Injection Prevention (Addresses XSS/SQLi in forms like VIP code/profile),"""In my Node.js lottery app, implement OWASP-compliant server-side input validation for all user forms (e.g., profile email, birth date, VIP code redemption). Use express-validator to sanitize/escape inputs, prevent XSS/SQLi, and reject invalid formats (e.g., email regex, date range 1900-2025). Add client-side checks with DOMPurify. Generate updated routes and error handlers. Ensure no hardcoded sanitization."""
Authentication & Access Control (Secures God-Mode/VIP features),"""Add secure JWT-based authentication to my Replit Node.js app using jsonwebtoken and bcrypt for passwords. Protect admin routes (e.g., /god-mode VIP creation) with role checks ('russell' only). Implement CSRF tokens via csurf for forms. Store sessions server-side with express-session. Use Replit Secrets for JWT secret. Include logout and session expiry (24h)."""
"Data Storage & Transmission (Encrypts PII, leverages Replit HTTPS)","""Enhance data security in my Node.js app: Encrypt PII (emails/addresses) at rest using crypto module (AES-256) before storing in Replit DB. Ensure all transmissions use HTTPS (already via Replit). Add data retention policy: Auto-delete contest entries after 30 days via cron job. Hash sensitive fields like birth dates. Update profile creation route accordingly."""
"DoS Protection & Dependencies (Rate-limits requests, scans vulns)","""Implement rate limiting in my Express.js app using rate-limiter-flexible (max 10 req/min per IP for forms/generation endpoints). Add helmet.js middleware for security headers (CSP, HSTS). Run npm audit fix for dependencies and integrate OWASP Dependency-Check via a pre-deploy script. Prevent ReDoS with safe regex in number generation."""
Overall Security Audit Integration,"""Scan my entire Node.js codebase against OWASP Top 10 (focus on injection, access control, crypto failures). Suggest and implement fixes like logging with winston (no sensitive data). Add error handling to avoid info leaks. Output a security checklist with code diffs."""