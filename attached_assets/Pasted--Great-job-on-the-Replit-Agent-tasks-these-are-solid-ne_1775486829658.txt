**Great job on the Replit Agent tasks — these are solid next steps for turning LotteryPro into a native iOS app while staying compliant with App Store rules (no external payment processors like Stripe/PayPal in the native shell).**

I've reviewed both tasks (#8 and #9) against your existing Vite + React + TypeScript stack, the full spec, and current best practices (as of April 2026). Here's my **refined, ready-to-paste prompts** for the Replit Agent. They are more precise, safer for Replit's Linux environment, and include safeguards so nothing breaks the live web app at lotterypro.app.

Use them **in order** (do #8 first, then #9). Each prompt is self-contained and references the "Done looks like" criteria you provided.

### Refined Prompt for #8 — Capacitor iOS Setup & Configuration
```
You are working on LotteryPro (React 18 + TypeScript + Vite frontend in /client, Express backend on port 5000, deployed on Replit).

Implement Capacitor iOS setup exactly as specified — prepare the codebase for later macOS build without running native commands here.

Tasks in order:
1. Install the required packages using npm (do NOT edit package.json manually):
   - @capacitor/core
   - @capacitor/cli
   - @capacitor/ios

   Run the proper install command and confirm they appear in dependencies and devDependencies where appropriate.

2. Create a new file capacitor.config.ts at the project ROOT (same level as vite.config.ts and package.json) with this exact content (use TypeScript):

import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.lotterypro.app',
  appName: 'LotteryPro',
  webDir: 'dist',
  server: {
    url: 'https://lotterypro.app',  // production fallback
    cleartext: true  // for local dev testing when needed
  },
  // Comment explaining Replit limitation and next steps:
  // NOTE: This project is on Replit (Linux). Do NOT run 'npx cap add ios' here — it requires macOS + Xcode.
  // After these changes, on a Mac: npm run build && npx cap sync ios && npx cap open ios
  // The Vite build must output to 'dist/' for cap sync to work.
};

export default config;

3. Verify the existing build chain:
   - Confirm that "npm run build" (or the Vite build script) outputs to the 'dist' folder.
   - If needed, ensure vite.config.ts has correct build.outDir set to 'dist' (do not change unless it's wrong).
   - Do NOT modify any existing scripts in package.json that would break the web deployment.

4. Update client/src/main.tsx or App.tsx only if necessary to ensure the root component mounts cleanly (no breaking changes).

After all changes:
- The web app at lotterypro.app must continue to work unchanged.
- Running "npm run build" should still succeed and produce dist/ correctly.
- Add a comment in the root README.md or capacitor.config.ts: "iOS native shell prepared — build on macOS with: npm run build && npx cap sync ios"

Output a summary of what was changed and any next manual steps for the macOS build.
```

### Refined Prompt for #9 — iOS Platform Detection & Monetization Compliance
```
Continue from the completed Capacitor iOS setup in LotteryPro.

Implement platform detection and iOS-specific compliance for payments and external links. Keep ALL web behavior 100% unchanged.

First, install the required plugins using npm:
- @capacitor/browser
- @capawesome-team/capacitor-purchases   (this is the modern Capacitor IAP plugin; do not use deprecated alternatives)

Then:

1. Create a platform utility hook: client/src/hooks/usePlatform.ts (or in lib/ if preferred)

import { Capacitor } from '@capacitor/core';
import { useState, useEffect } from 'react';

export const usePlatform = () => {
  const [platform, setPlatform] = useState<'web' | 'ios' | 'android'>('web');

  useEffect(() => {
    setPlatform(Capacitor.getPlatform() as 'web' | 'ios' | 'android');
  }, []);

  return platform;
};

2. Update payment pages (client/src/pages/subscription.tsx and client/src/pages/pricing.tsx):
   - Import usePlatform.
   - Wrap existing Stripe + PayPal components in: {platform === 'web' && <StripePayPalUI />}
   - When platform === 'ios', render a clean placeholder component instead:
     Something like:
     <div className="ios-iap-placeholder">
       <h3>Subscriptions & Credits Available on iOS</h3>
       <p>Unlock unlimited generations and premium features via Apple In-App Purchases.</p>
       <button onClick={handleIAP}>Unlock with Apple</button>
     </div>
   - Stub the IAP call using @capawesome-team/capacitor-purchases (example: Purchases.getProducts() or purchaseProduct with placeholder IDs like 'pro_monthly'). Log to console for now — no real transactions.

3. Create a helper for external links: client/src/lib/openExternal.ts

import { Browser } from '@capacitor/browser';
import { Capacitor } from '@capacitor/core';

export const openExternal = async (url: string) => {
  if (Capacitor.getPlatform() === 'ios' || Capacitor.getPlatform() === 'android') {
    await Browser.open({ url, presentationStyle: 'popover' });  // or 'fullscreen'
  } else {
    window.open(url, '_blank', 'noopener,noreferrer');
  }
};

4. Update all external link calls:
   - In client/src/pages/books.tsx (Amazon book links for craps and slot machines books)
   - In client/src/pages/music-home.tsx (Apple Music, Spotify, YouTube links for Russell Nomer's catalog)
   - Replace every window.open(url, '_blank') with openExternal(url)
   - Also check client/src/pages/App.tsx or any shared components for similar links.

5. Add platform check in any global components if needed (e.g., PostGenerationModal music bar).

Requirements:
- No regressions on web: Stripe Checkout, PayPal buttons, and all window.open must work exactly as before on lotterypro.app.
- On iOS (when built): payments show IAP placeholder only; external links open in native Safari (not in-app webview).
- Use shadcn/ui styling for the IAP placeholder to match the app.
- Add clear comments explaining App Store compliance (no third-party payments in native iOS build).

After changes, confirm with a test build (npm run build) that nothing is broken on the web side.
```

**Key Advice Before Pasting These:**

- Run them **one at a time** and test the web app thoroughly after each (especially subscription/pricing pages and music/books links).
- Replit can't build the actual .ipa — you'll need to:
  1. Push the project to GitHub after these changes.
  2. Clone on a Mac with Xcode installed.
  3. Run `npm install`, `npm run build`, `npx cap add ios` (only on Mac), then `npx cap sync ios` and open in Xcode.
- For IAP: The @capawesome-team/capacitor-purchases plugin is a good modern choice. You'll still need to set up real products in App Store Connect later (separate task).
- This setup keeps your web revenue (Stripe/PayPal) intact while making the native iOS version compliant.

These prompts minimize risk, follow Capacitor best practices, and directly support your goals of wider adoption (native app in App Store) + continued music/book promotion.

Paste **#8 refined prompt first**, let the agent finish, then verify with `npm run build`. Tell me the results or any errors, and we'll move to #9 or fix anything.

This is a big step toward a native LotteryPro app that drives more downloads, subscriptions (via IAP), and exposure for your music + Amazon books. Let's keep momentum!